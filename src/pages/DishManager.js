import React from "react";
import { makeStyles, withStyles, Typography, Grid } from "@material-ui/core";

const DishManager = () => {
  return (
    <Grid container item>
      <Grid item>
        <Typography variant="h3">Dish Manager</Typography>
      </Grid>
    </Grid>
  );
};

export default DishManager;
